node_modules
dist
.env
.env.local
.DS_Store
*.local
.mcp.json
.cursor/mcp.json
.vscode/mcp.json
opencode.json

# Local agent settings
.claude/settings.local.json
.vercel
.env*

# --- machine-local agent + secret config (uniform block) ---
# .env, .env.*, .mcp.json, opencode.json, .vscode/mcp.json and .vercel
# are already covered above and are deliberately not repeated here.
!.env.example
.aethereum*
.claude/
.codex/
.cursor/
*-ENGINEERPROMPT.md
AGENTS.md
GEMINI.md
